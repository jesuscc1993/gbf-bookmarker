{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile SASS",
      "type": "shell",
      "command": "sass ${fileDirname}/${fileBasenameNoExtension}.scss ${fileDirname}/${fileBasenameNoExtension}.css --style compressed",
      "group": "build",
      "problemMatcher": []
    }
  ]
}
